[project]
name = "godwit-vision"
version = "0.1.0"
description = "Vision Board server: datasources, dashboards, guardrails, actions (L10)."
readme = "HANDOFF.md"
requires-python = ">=3.12,<3.13"
dependencies = ["godwit-contracts"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/godwit_vision"]
