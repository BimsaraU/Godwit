# godwit-guard -- HANDOFF

**Owner:** A5
**Plane:** boundary
**Purpose:** Classification, redaction, tokenisation and the egress gate (GATE).

> Stub written by A0. The owning agent replaces this file in full.
> Read `docs/ARCHITECTURE.md` first, then `docs/packages/godwit-contracts.md`.

**This package IS the boundary.** It is the only legitimate producer of
`SafePayload` and the only writer of `EgressRecord`.

## Status

Not started.

## Known limits

- Everything.
