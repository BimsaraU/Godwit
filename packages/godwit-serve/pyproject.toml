[project]
name = "godwit-serve"
version = "0.1.0"
description = "Artifact registry, provisioning, shadow deploy, drift and rollback (L8)."
readme = "HANDOFF.md"
requires-python = ">=3.12,<3.13"
dependencies = ["godwit-contracts"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/godwit_serve"]
