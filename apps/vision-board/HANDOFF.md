# apps/vision-board -- HANDOFF

**Owner:** A17
**Plane:** control (browser)
**Purpose:** Vision Board UI. React 18 + TypeScript + Vite + Tailwind + TanStack Query + uPlot.

> Stub written by A0. A17 replaces this file in full.

The UI is an egress destination (`Destination.UI`). Everything it renders arrived
through godwit-guard. It must never call a data-plane service directly.

## Status

Not started. A0 deliberately did not scaffold `package.json`, so A17 picks the exact
toolchain versions in one place.

## Known limits

- Everything.
